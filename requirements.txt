# [minimal]
# MINIMAL CORE - CLI only
click>=8.1.0
rich>=13.0.0
psutil>=5.9.0
pyyaml>=6.0
nvidia-ml-py
requests>=2.31.0

# [normal]
# WEB UI (optional)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
win10toast
winrt-Windows.UI.Notifications
winrt-Windows.Data.Xml.Dom

# VISUALIZATION (optional)
pygame>=2.5.0
numpy>=1.24.0

# [full]
# GPU ACCELERATION (CUDA 12.x)
cupy-cuda12x
torch

# Use this single line to install all
# one-line installer (kept commented to avoid being treated as a package)
# uv pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu128 --index-strategy unsafe-best-match



